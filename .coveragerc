# -*- mode: conf -*-

[report]
# Regexes for lines to exclude from consideration
exclude_lines =
    # Have to re-enable the standard pragma
    pragma: no cover

    # Don't complain about imports. Django is too weird for that.
    (from .* )?import .*

